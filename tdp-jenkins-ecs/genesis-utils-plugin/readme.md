# Genesis Utils Plugin

Plugin Jenkins que expone dependencias necesarias para las Shared Libraries Genesis.

Dependencias incluidas:

- Apache Commons DBCP2
- PostgreSQL JDBC
- JSoup
- SnakeYAML

## Compilar

```bash
mvn clean package
mvn clean package -U