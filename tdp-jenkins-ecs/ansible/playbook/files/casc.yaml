jenkins:
  systemMessage: "Jenkins Docker Setup with SWR"
  numExecutors: 0

  securityRealm: none
  authorizationStrategy: unsecured

  clouds:
    - docker:
        name: "docker"

        dockerApi:
          dockerHost:
            uri: "unix:///var/run/docker.sock"

        templates:
          - labelString: "agent-terraform"
            name: "agent-terraform"
            mode: EXCLUSIVE
            pullStrategy: PULL_ALWAYS
            retentionStrategy:
              idleMinutes: 5
            remoteFs: "/home/jenkins"
            connector:
              attach:
                user: "jenkins"
            dockerTemplateBase:
              image: "swr.la-south-2.myhuaweicloud.com/cce-jenkins-integration-organization/jenkins-agent-terraform:1.0"
              pullCredentialsId: "swr-jenkins"
              tty: true
              mounts:
                - "type=bind,source=/opt/jenkins/workspaces,target=/home/jenkins/workspace"

          - labelString: "agent-huawei"
            name: "agent-huawei"
            mode: EXCLUSIVE
            pullStrategy: PULL_ALWAYS
            retentionStrategy:
              idleMinutes: 5
            remoteFs: "/home/jenkins"
            connector:
              attach:
                user: "jenkins"
            dockerTemplateBase:
              image: "swr.la-south-2.myhuaweicloud.com/cce-jenkins-integration-organization/jenkins-agent-huawei:1.0"
              pullCredentialsId: "swr-jenkins"
              tty: true
              mounts:
                - "type=bind,source=/opt/jenkins/workspaces,target=/home/jenkins/workspace"

          - labelString: "genesisBackJdk21"
            name: "genesisBackJdk21"
            mode: EXCLUSIVE
            pullStrategy: PULL_ALWAYS
            retentionStrategy:
              idleMinutes: 5
            remoteFs: "/home/jenkins"
            connector:
              attach:
                user: "jenkins"
            dockerTemplateBase:
              image: "swr.la-south-2.myhuaweicloud.com/cce-jenkins-integration-organization/jenkins-genesisbackjdk21:1.0"
              pullCredentialsId: "swr-jenkins"
              tty: true
              mounts:
                - "type=bind,source=/opt/jenkins/workspaces,target=/home/jenkins/workspace"
                - "type=bind,source=/opt/jenkins/.m2,target=/home/jenkins/.m2"

          - labelString: "genesisFrontNodeLTS"
            name: "genesisFrontNodeLTS"
            mode: EXCLUSIVE
            pullStrategy: PULL_ALWAYS
            retentionStrategy:
              idleMinutes: 5
            remoteFs: "/home/jenkins"
            connector:
              attach:
                user: "jenkins"
            dockerTemplateBase:
              image: "swr.la-south-2.myhuaweicloud.com/cce-jenkins-integration-organization/jenkins-genesisfrontnodelts:1.0"
              pullCredentialsId: "swr-jenkins"
              tty: true
              mounts:
                - "type=bind,source=/opt/jenkins/workspaces,target=/home/jenkins/workspace"
                - "type=bind,source=/opt/jenkins/.m2,target=/home/jenkins/.m2"

          - labelString: "genesisDeploySecureLTS"
            name: "genesisDeploySecureLTS"
            mode: EXCLUSIVE
            pullStrategy: PULL_ALWAYS
            retentionStrategy:
              idleMinutes: 5
            remoteFs: "/home/jenkins"
            connector:
              attach:
                user: "jenkins"
            dockerTemplateBase:
              image: "swr.la-south-2.myhuaweicloud.com/cce-jenkins-integration-organization/jenkins-genesisdeploysecurelts:1.0"
              pullCredentialsId: "swr-jenkins"
              tty: true
              extraGroups:
                - "988"
              mounts:
                - "type=bind,source=/opt/jenkins/workspaces,target=/home/jenkins/workspace"
                - "type=bind,source=/opt/jenkins/.m2,target=/home/jenkins/.m2"
                - "type=bind,source=/var/run/docker.sock,target=/var/run/docker.sock"

          - labelString: "genesisHuawei"
            name: "genesisHuawei"
            mode: EXCLUSIVE
            pullStrategy: PULL_ALWAYS
            retentionStrategy:
              idleMinutes: 5
            remoteFs: "/home/jenkins"
            connector:
              attach:
                user: "jenkins"
            dockerTemplateBase:
              image: "swr.la-south-2.myhuaweicloud.com/cce-jenkins-integration-organization/jenkins-genesishuawei:1.0"
              pullCredentialsId: "swr-jenkins"
              tty: true
              extraGroups:
                - "988"
              mounts:
                - "type=bind,source=/opt/jenkins/workspaces,target=/home/jenkins/workspace"
                - "type=bind,source=/opt/jenkins/.m2,target=/home/jenkins/.m2"
                - "type=bind,source=/var/run/docker.sock,target=/var/run/docker.sock"

          - labelString: "genesisSecrets"
            name: "genesisSecrets"
            mode: EXCLUSIVE
            pullStrategy: PULL_ALWAYS
            retentionStrategy:
              idleMinutes: 5
            remoteFs: "/home/jenkins"
            connector:
              attach:
                user: "jenkins"
            dockerTemplateBase:
              image: "swr.la-south-2.myhuaweicloud.com/cce-jenkins-integration-organization/jenkins-genesissecrets:1.0"
              pullCredentialsId: "swr-jenkins"
              tty: true
              mounts:
                - "type=bind,source=/opt/jenkins/workspaces,target=/home/jenkins/workspace"

          - labelString: "genesisImageScan"
            name: "genesisImageScan"
            mode: EXCLUSIVE
            pullStrategy: PULL_ALWAYS
            retentionStrategy:
              idleMinutes: 5
            remoteFs: "/home/jenkins"
            connector:
              attach:
                user: "jenkins"
            dockerTemplateBase:
              image: "swr.la-south-2.myhuaweicloud.com/cce-jenkins-integration-organization/jenkins-genesisimagescan:1.0"
              pullCredentialsId: "swr-jenkins"
              tty: true
              extraGroups:
                - "988"
              mounts:
                - "type=bind,source=/opt/jenkins/workspaces,target=/home/jenkins/workspace"
                - "type=bind,source=/var/run/docker.sock,target=/var/run/docker.sock"

          - labelString: "dockerNotifier"
            name: "dockerNotifier"
            mode: EXCLUSIVE
            pullStrategy: PULL_ALWAYS
            retentionStrategy:
              idleMinutes: 5
            remoteFs: "/home/jenkins"
            connector:
              attach:
                user: "jenkins"
            dockerTemplateBase:
              image: "swr.la-south-2.myhuaweicloud.com/cce-jenkins-integration-organization/jenkins-dockernotifier:1.0"
              pullCredentialsId: "swr-jenkins"
              tty: true
              mounts:
                - "type=bind,source=/opt/jenkins/workspaces,target=/home/jenkins/workspace"

unclassified:
  location:
    url: "http://jenkins:8080/"
    adminAddress: "admin@localhost"

credentials:
  system:
    domainCredentials:
      - credentials:

          - usernamePassword:
              id: github-creds
              scope: GLOBAL
              username: ${GITHUB_USER}
              password: ${GITHUB_TOKEN}
              description: GitHub credentials

          - usernamePassword:
              id: telefonica-bitbucket
              scope: GLOBAL
              username: ""
              password: ""
              description: Telefonica Bit Bucket Credentials

          - usernamePassword:
              id: swr-jenkins
              scope: GLOBAL
              username: ${SWR_USER}
              password: ${SWR_PASSWORD}
              description: SWR Login Jenkins

          - string:
              id: hwc-access-key
              scope: GLOBAL
              secret: ${HWC_ACCESS_KEY}
              description: Huawei Cloud Access Key

          - string:
              id: hwc-secret-key
              scope: GLOBAL
              secret: ${HWC_SECRET_KEY}
              description: Huawei Cloud Secret Key

          - file:
              id: cce-app-kubeconfig
              scope: GLOBAL
              fileName: kubeconfig
              secretBytes: ""
              description: CCE Kubeconfig file for App

          - file:
              id: sops-gpg-key
              scope: GLOBAL
              fileName: sops-gpg-key
              secretBytes: ""
              description: Key for SOPS