replicaCount: 1

image:
  registry: swr.la-south-2.myhuaweicloud.com
  namespace: cce-basic-app
  repository: jenkins-hwc
  tag: "2.0"
  pullPolicy: IfNotPresent

imagePullSecrets:
  - name: default-secret

service:
  type: LoadBalancer
  port: 80
  targetPort: 8080

elb:
  id: ""

jenkins:
  javaOpts: "-Djenkins.install.runSetupWizard=false"
