apiVersion: v1
kind: Service
metadata:
  name: demo-stable
  namespace: rollout-demo
spec:
  selector:
    app: demo
  ports:
  - port: 80
    targetPort: 5678

---
apiVersion: v1
kind: Service
metadata:
  name: demo-canary
  namespace: rollout-demo
spec:
  selector:
    app: demo
  ports:
  - port: 80
    targetPort: 5678
