apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: demo-route
spec:
  parentRefs:
  - name: demo-gateway

  rules:
  - backendRefs:
    - name: demo-stable
      port: 80
      weight: 100
    - name: demo-canary
      port: 80
      weight: 0