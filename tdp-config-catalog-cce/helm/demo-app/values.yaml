configServer:
  replicaCount: 1
  port: 8888
  image:
    registry: swr.la-south-2.myhuaweicloud.com
    organization: cce-jenkins-integration-organization
    repository: config-server-hw
    #tag: "1.2.3" #usando agency
    tag: "1.2.4" #usando AK y SK
    pullPolicy: Always
  secretName: ms-app-config-server-keyvault-secrets
  enabled: false

catalogError:
  replicaCount: 1
  port: 50051
  image:
    registry: swr.la-south-2.myhuaweicloud.com
    organization: cce-jenkins-integration-organization
    repository: catalog-error-hw
    tag: "1.0"
    pullPolicy: Always
  secretName: ms-app-config-server-keyvault-secrets
  enabled: false

demoClient:
  replicaCount: 1
  image:
    registry: swr.la-south-2.myhuaweicloud.com
    organization: cce-jenkins-integration-organization
    repository: demo-client
    tag: "1.0"
    pullPolicy: Always
  secretName: ms-app-config-server-keyvault-secrets
  port: 80
  targetPort: 8081
  enabled: false

# Se inyecta dinámicamente desde Jenkins
secretsStoreCreds:
  access_key: ""  
  secret_key: ""  
  enabled: true

elb:
  id: ""                